# Vault DON secrets are declared inline in main.ts via vaultDonSecrets[] and uploaded
# separately with: cre secrets create cre/loan --target staging-settings
# Secret name: DAIRY_API_KEY
# Value: the API token obtained by the Dynamic server wallet via x402 payment
secretsNames: {}
